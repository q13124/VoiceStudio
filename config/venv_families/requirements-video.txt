# VoiceStudio Video Family (venv_video)
# Engines: SadTalker, FOMM, Deforum, SVD, DeepFaceLab, MoviePy, FFmpeg-AI, Video Creator
# Estimated size: ~10GB

# Core framework
torch>=2.2.2
torchvision>=0.17.0
numpy>=1.24.0,<2.0.0

# Face detection and alignment
face-alignment>=1.4.1
dlib>=19.24.0
mediapipe>=0.10.0

# Video processing
moviepy>=1.0.3
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
opencv-python>=4.8.0

# SadTalker / Lip sync
gfpgan>=1.3.8
facexlib>=0.3.0

# Diffusers (for SVD)
diffusers>=0.27.0
transformers>=4.35.0
accelerate>=0.27.0
safetensors>=0.4.0

# Image processing
Pillow>=10.0.0
scipy>=1.10.0

# Audio processing (for lip sync)
librosa>=0.10.0
soundfile>=0.12.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
requests>=2.28.0
ffmpeg-python>=0.2.0

# GPU support
--extra-index-url https://download.pytorch.org/whl/cu121
