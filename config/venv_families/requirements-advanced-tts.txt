# VoiceStudio Advanced TTS Family (venv_advanced_tts)
# Engines: Chatterbox, F5-TTS, OpenVoice, GPT-SoVITS
# Estimated size: ~10GB
# Note: May need nightly torch for SM 120 (RTX 5000 series)

# Core framework - Chatterbox requires torch==2.8.0
# For SM 120 support, install nightly manually after base install
torch==2.8.0
torchaudio==2.6.0
numpy>=1.24.0,<2.0.0

# Chatterbox TTS (zero-shot voice cloning)
chatterbox-tts>=1.0.0

# F5-TTS
# f5-tts>=0.1.0  # Uncomment when available on PyPI

# OpenVoice
# openvoice>=0.1.0  # Uncomment when available on PyPI

# GPT-SoVITS dependencies
fairseq>=0.12.0
pyworld>=0.3.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0

# ML utilities
transformers>=4.35.0
safetensors>=0.4.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0

# GPU support
--extra-index-url https://download.pytorch.org/whl/cu124
