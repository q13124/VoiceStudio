# VoiceStudio Image Family (venv_image)
# Engines: SDXL, SD-CPU, FastSD-CPU, Fooocus, OpenJourney, Realistic Vision, RealESRGAN, SDNext, LocalAI
# Estimated size: ~12GB

# Core framework
torch>=2.2.2
torchvision>=0.17.0
numpy>=1.24.0,<2.0.0

# Diffusers ecosystem
diffusers>=0.27.0
transformers>=4.35.0
accelerate>=0.27.0
safetensors>=0.4.0

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0
imageio>=2.31.0

# Model optimization
xformers>=0.0.24
bitsandbytes>=0.42.0

# RealESRGAN / Upscaling
realesrgan>=0.3.0
basicsr>=1.4.2

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
requests>=2.28.0
huggingface-hub>=0.20.0

# GPU support
--extra-index-url https://download.pytorch.org/whl/cu121
