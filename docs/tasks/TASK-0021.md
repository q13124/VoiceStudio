# TASK-0021: OpenMemory MCP Wiring (Phase 6+)

## Objective

Complete the stubbed OpenMemory MCP protocol integration in `tools/context_manager/memory_adapter.py`. Enable Context Manager to use OpenMemory MCP for persistent memory storage and retrieval when available.

## Affected Modules

- [x] `tools/context/sources/memory_adapter.py` — Complete `_try_openmemory_mcp_protocol()` implementation
- [ ] `tools/context/README.md` — Document OpenMemory MCP integration (if exists)
- [ ] `.cursor/hooks.json` — Verify context_manager hook configuration
- [ ] `docs/governance/CONTEXT_MANAGER_INTEGRATION.md` — Update with OpenMemory MCP usage

## Constraints

- OpenMemory MCP must be available (check `C:\Users\Tyler\.cursor\projects\e-VoiceStudio\mcps\user-openmemory\`).
- Integration must be optional (graceful degradation if MCP unavailable).
- No breaking changes to existing Context Manager API.
- Follow `.cursor/rules/openmemory.mdc` for memory storage patterns.

## Required Proofs

- [ ] `_try_openmemory_mcp_protocol()` implementation complete (no stub)
- [ ] Unit tests for OpenMemory MCP integration (or integration test)
- [ ] Context Manager successfully stores/retrieves memory via MCP when available
- [ ] Graceful fallback when MCP unavailable (no crash)

## Acceptance Criteria

- [ ] OpenMemory MCP protocol wiring complete in memory_adapter.py.
- [ ] Context Manager can store and retrieve memory via OpenMemory MCP.
- [ ] Tests pass (unit or integration).
- [ ] Documentation updated (Context Manager integration guide).

## Tech Debt

- None (this is a Phase 6+ enhancement, not tech debt).

## Owner

- **Role 4 (Core Platform)** or **Role 1 (System Architect)** — Context Manager ownership.

## Status

- [ ] Not Started
- [ ] In Progress
- [ ] Blocked
- [x] Complete

**Notes**: 
- 2026-01-30: Initial implementation.
- 2026-02-04: Fixed tool name (`openmemory_query` instead of `search-memory`), added multi-provider support (openmemory + mem0), improved response parsing. MemorySourceAdapter includes full MCP protocol support with graceful fallback.
