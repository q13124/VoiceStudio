# VS-0023 Handoff — Release Build Configuration Hotfix

**Date:** 2025-01-28
**Role:** Build & Tooling Engineer
**Gate:** C
**State:** DONE
**Last verified:** 2026-01-10

---

## Summary

Build & Tooling Engineer must deliver a working Release build configuration to unblock VS-0003 (installer verification) and VS-0012 (launch crash testing). The Release Engineer cannot complete their packaging and launch verification tasks without a functional Release binary.

**Current status (2026-01-10):**

- Debug + Release builds succeed (x64)
- Gate C publish+launch script succeeds (`scripts/gatec-publish-launch.ps1`)
- CI build lane runs RuleGuard + Debug/Release builds + Gate C publish check (see `.github/workflows/build.yml`)

---

## Context

- **VS-0003 (Gate H)**: Release Engineer needs working Release binary for installer creation and upgrade/rollback testing
- **VS-0012 (Gate C)**: Release Engineer needs working Release binary for launch crash verification (COMException 0x80040154)
- **Current Status**: Debug builds work, Release builds fail or produce non-functional binaries
- **Impact**: Packaging deliverables and launch testing completely blocked

---

## Assignment

**Owner:** Build & Tooling Engineer
**Reviewer:** Overseer
**Priority:** Critical (blocks Release Engineer progress)

Deliver a Release build configuration that:

1. Builds successfully with same quality as Debug
2. Produces a launchable binary (no COMException 0x80040154)
3. Enables Release Engineer to complete VS-0003 and VS-0012

---

## Investigation Required

1. **Compare Debug vs Release builds:**

   - What differs between configurations?
   - Are there conditional compilation flags?
   - Different optimization settings?

2. **Check build outputs:**

   - Release binary size vs Debug
   - Dependency inclusion differences
   - Runtime requirements differences

3. **Verify launch environment:**

   - Does Release binary have different runtime requirements?
   - WinUI runtime activation differences?

4. **Resolve build tooling issues:**
   - Kill blocking dotnet-dump process (PID 22992) that's locking DLL during publish
   - Clean publish output directory before retry
   - Verify XamlParseException at MainWindow.InitializeComponent (restored toolbar didn't fix)

---

## Success Criteria

- `dotnet build "E:\VoiceStudio\VoiceStudio.sln" -c Release -p:Platform=x64` succeeds
- Release binary launches: `Start-Process "E:\VoiceStudio\.buildlogs\x64\Release\net8.0-windows10.0.19041.0\VoiceStudio.App.exe"`
- Window appears without COMException 0x80040154
- Release Engineer can proceed with VS-0003 and VS-0012

---

## Files to Check

- `VoiceStudio.sln` - Configuration definitions
- `Directory.Build.props` - Build properties
- `Directory.Build.targets` - Build targets
- `.buildlogs/x64/Release/` - Build output location
- `src/VoiceStudio.App/VoiceStudio.App.csproj` - Project configuration

---

## Latest publish investigation (2026-01-05)

- Project defaults now set to unpackaged apphost EXE: `WindowsPackageType=None`, `EnableMsixTooling=false`, `EnableDefaultPriItems=false`, `GenerateAppxPackageOnBuild=false`, `AppxManifest` cleared (MSIX lane removed/archived; see `docs/archive/msix/`).
- Publish settings (fast path, unpackaged EXE): `dotnet publish src/VoiceStudio.App/VoiceStudio.App.csproj -c Release -r win-x64 -p:SelfContained=true -p:UseAppHost=true -p:WindowsPackageType=None -p:EnableMsixTooling=false -p:EnableDefaultPriItems=false -p:PublishSingleFile=false -p:PublishDir=E:\VoiceStudio\.buildlogs\x64\Release\apphost-flat\ /bl:E:\VoiceStudio\.buildlogs\x64\Release\apphost-flat\publish.binlog`.
- Output: apphost-produced `VoiceStudio.App.exe` at `E:\VoiceStudio\.buildlogs\x64\Release\apphost-flat\VoiceStudio.App.exe` (278,528 bytes); runtimeconfig/deps alongside.
- Runtime defaults: `backend/api/main.py` now sets `VOICESTUDIO_MODELS_PATH` to `E:\VoiceStudio\models` (HF/TTS/whisper/piper subfolders) unless overridden; ensure model assets live under that root.
- Current status: unpackaged apphost publish/launch is green with model assets in place; binlogs retained under `.buildlogs/` (e.g., `E:\VoiceStudio\build.binlog` and `E:\VoiceStudio\.buildlogs\x64\Release\...\publish.binlog`).
- Binlog: `E:\VoiceStudio\.buildlogs\x64\Release\apphost-flat\publish.binlog`; detailed log: `...apphost-flat\publish.detailed.log` shows `_CreateAppHost` executed and copy into publish dir.
- Runtime check: launching the EXE via `Start-Process ...\VoiceStudio.App.exe -PassThru` exits with code `-1073740286` (0xC0000602). Event Viewer Application log shows CoreMessagingXP.dll as the faulting module (`Exception code: 0xc0000602`, module path `...apphost-flat\CoreMessagingXP.dll`, ReportId `f4740580-dc53-4740-b23e-2605265a5169`), no package identity.
- Observation: publish outputs (apphost-flat, apphost-sc-msix) do not contain `Microsoft.WindowsAppRuntime.*` bootstrap/redist files even with `<WindowsAppSDKSelfContained>true</WindowsAppSDKSelfContained>`; `publish.detailed.log` shows `_CreateAppHost` and WindowsAppSDKSelfContained manifest merge but runtime redist not copied.
- MSIX lane has been removed/archived to eliminate dual-path ambiguity; do not switch to MSIX. Historical MSIX artifacts live under `docs/archive/msix/`.

### Gate C script attempt (2026-01-05)

- Command: `powershell -ExecutionPolicy Bypass -File scripts\gatec-publish-launch.ps1`
- Binlog: `E:\VoiceStudio\build.binlog`
- Result: **FAILED during build** (XAML compiler exit code 1) due to existing CS0126 errors in `src/VoiceStudio.App/Views/Panels/LibraryView.xaml.cs` (several async handlers require `Task` return). Also showed prior nullability warnings. No launch executed. Requires UI engineer fix.

---

## Timeline

- **Target:** Complete within 1-2 days
- **Unblocks:** VS-0003 (installer), VS-0012 (launch), UI test framework setup
- **Next:** Release Engineer takes over for packaging verification

---

## Related Ledger Entries

- VS-0024: CS0126 compilation errors in LibraryView.xaml.cs (blocks this)
- VS-0025: CoreMessagingXP.dll crash on Release publish launch (blocks this)

---

## Proof run (2026-01-10)

```powershell
cd E:\VoiceStudio
python tools\verify_no_stubs_placeholders.py
dotnet build "E:\VoiceStudio\VoiceStudio.sln" -c Debug -p:Platform=x64 /bl:"E:\VoiceStudio\.buildlogs\ci-local-debug.binlog"
dotnet build "E:\VoiceStudio\VoiceStudio.sln" -c Release -p:Platform=x64 /bl:"E:\VoiceStudio\.buildlogs\ci-local-release.binlog"
.\scripts\gatec-publish-launch.ps1 -Configuration Release -RuntimeIdentifier win-x64 -SmokeSeconds 10
```

**Result:** ✅ Debug build OK, ✅ Release build OK, ✅ Gate C publish+launch OK (`ExitCode: 0`).  
See `.buildlogs\gatec-latest.txt` for the exact per-run publish binlog path (`gatec-publish-<timestamp>-<pid>.binlog`).
