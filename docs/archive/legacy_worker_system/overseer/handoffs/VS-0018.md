# VS-0018 Handoff — RuleGuard violation in /api/engines stop endpoint (remove pass)

**Date:** 2026-01-01  
**Role:** System Architect  
**Gate:** B  
**State:** DONE

---

## Summary

Removed a RuleGuard-blocking `pass` statement in `backend/api/routes/engines.py` inside the engine lifecycle stop endpoint and replaced it with concrete behavior:

- If `job_id` is provided: release the engine lease.
- If `job_id` is not provided: request a graceful drain/stop (immediate if unleased; deferred if leased).

This restores Gate B determinism (RuleGuard pass) and makes the engine stop semantics explicit and usable by the frontend adapter.

---

## Changes

- `backend/api/routes/engines.py`
  - Updated `POST /api/engines/{engine_id}/stop` to remove `pass`
  - Implemented drain vs lease-release behavior

---

## Proof Run

```bash
python tools\verify_no_stubs_placeholders.py
```

**Result:**
- ✅ RuleGuard: No violations found

