<UserControl x:Class="VoiceStudio.App.Views.Panels.AdvancedRealTimeVisualizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:VoiceStudio.App.Controls">
  <!-- Phase 0: keep XAML minimal to avoid XamlCompiler.exe crashes. -->
  <Grid AutomationProperties.AutomationId="AdvancedRealTimeVisualizationView_Root">
    <controls:HelpOverlay x:Name="HelpOverlay" IsVisible="False" Visibility="Collapsed"/>
    <StackPanel Spacing="12" Padding="16">
      <TextBlock Text="Advanced Real-Time Visualization"
                 FontWeight="SemiBold"
                 FontSize="18" />
      <TextBlock Text="Temporarily disabled for build stability."
                 Opacity="0.7"
                 TextWrapping="Wrap" />
    </StackPanel>
  </Grid>
</UserControl>
