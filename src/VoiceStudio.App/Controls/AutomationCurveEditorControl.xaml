<UserControl x:Class="VoiceStudio.App.Controls.AutomationCurveEditorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VoiceStudio.App.Controls"
    x:DataType="local:AutomationCurveEditorControl">
    <Grid x:Name="EditorRoot">
        <Path x:Name="CurvePath" Stroke="Cyan" StrokeThickness="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        <Canvas x:Name="PointsCanvas" />
        <TextBlock x:Name="EmptyText" Text="No curve data" HorizontalAlignment="Center" VerticalAlignment="Center"
            Opacity="0.6" Visibility="Collapsed" />
    </Grid>
</UserControl>