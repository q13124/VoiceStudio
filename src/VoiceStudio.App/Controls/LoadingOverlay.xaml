<UserControl x:Class="VoiceStudio.App.Controls.LoadingOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:VoiceStudio.App.Converters"
    xmlns:local="using:VoiceStudio.App.Controls"
    x:DataType="local:LoadingOverlay">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.15" EnableDependentAnimation="False">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>
    <Grid x:Name="LoadingGrid" Background="{StaticResource VSQ.Loading.BackgroundBrush}" Visibility="{x:Bind IsLoading, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
          AutomationProperties.AutomationId="LoadingOverlay_Root">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="{StaticResource VSQ.Spacing.Value.Medium}">
            <!-- Use WinUI implicit ProgressRing style; apply VSQ tokens via properties. -->
            <ProgressRing
                Width="{StaticResource VSQ.Icon.Size.Large}"
                Height="{StaticResource VSQ.Icon.Size.Large}"
                Foreground="{StaticResource VSQ.Loading.ForegroundBrush}"
                IsActive="True"
                AutomationProperties.AutomationId="LoadingOverlay_ProgressRing" />
            <TextBlock Text="{x:Bind LoadingMessage, Mode=OneWay}" Foreground="{StaticResource VSQ.Loading.TextBrush}" FontSize="{StaticResource VSQ.FontSize.Body}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" MaxWidth="300">
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
