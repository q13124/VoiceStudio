<UserControl
    x:Class="VoiceStudio.App.Controls.PanelPreviewPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border x:Name="PreviewBorder"
            Background="{StaticResource VSQ.Panel.Background.DarkBrush}"
            BorderBrush="{StaticResource VSQ.Panel.BorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12"
            MinWidth="280"
            MaxWidth="400"
            MaxHeight="300">
        <Border.Shadow>
            <ThemeShadow />
        </Border.Shadow>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <FontIcon x:Name="PreviewIcon" 
                         FontSize="24" 
                         Margin="0,0,8,0"
                         Foreground="{StaticResource VSQ.Accent.PrimaryBrush}"/>
                <TextBlock x:Name="PreviewTitle" 
                          Text="Panel Preview"
                          Style="{StaticResource VSQ.Text.Title}"
                          VerticalAlignment="Center"/>
            </StackPanel>
            <!-- Description -->
            <TextBlock x:Name="PreviewDescription"
                      Grid.Row="1"
                      Text="Panel description"
                      Style="{StaticResource VSQ.Text.Body}"
                      TextWrapping="Wrap"
                      Margin="0,0,0,12"
                      Foreground="{StaticResource VSQ.Text.SecondaryBrush}"/>
            <!-- Preview Content -->
            <Border Grid.Row="2"
                   Background="{StaticResource VSQ.Background.DarkerBrush}"
                   BorderBrush="{StaticResource VSQ.Panel.BorderBrush}"
                   BorderThickness="1"
                   CornerRadius="4"
                   Padding="8"
                   MaxHeight="180">
                <ScrollViewer VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
                    <ContentPresenter x:Name="PreviewContentPresenter"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</UserControl>
