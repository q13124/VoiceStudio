<UserControl x:Class="VoiceStudio.App.Controls.AutomationCurvesEditorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VoiceStudio.App.Controls"
    x:DataType="local:AutomationCurvesEditorControl">
    <Grid x:Name="Root">
        <Path x:Name="CurvePath" Stroke="Cyan" StrokeThickness="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        <TextBlock x:Name="EmptyText" Text="Select a curve to edit" HorizontalAlignment="Center" VerticalAlignment="Center"
            Opacity="0.6" Visibility="Collapsed" />
    </Grid>
</UserControl>