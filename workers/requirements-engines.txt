# Core
torch==2.9.*
torchaudio==2.9.*
numpy<2.0
soundfile
numba
# Codec / IO
torchcodec==0.8.*
ffmpeg-python
# ASR
faster-whisper==1.0.3
ctranslate2==4.6.*
# TTS
TTS==0.22.0         # includes XTTS-v2 in coqui-tts
openvoice==2.0.*    # OpenVoice V2 (ensure you've placed models)
cosyvoice2==0.1.*   # If unavailable on pip, keep as local; code checks
# Diarization
pyannote.audio==4.0.1
pyannote.core==5.0.0
# Utils
pydub
scipy
