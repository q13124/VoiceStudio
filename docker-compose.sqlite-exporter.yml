version: '3.9'
services:
  sqlite-exporter:
    build:
      context: ./ops/sqlite_metrics_exporter
    environment:
      - DB_URL=${DB_URL:-sqlite:///./app.db}
    ports: ["8001:8001"]
    volumes:
      - ./app.db:/app/app.db  # or mount your DB volume
    restart: unless-stopped
