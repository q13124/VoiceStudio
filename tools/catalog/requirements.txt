# Requirements for VoiceStudio Plugin Catalog tools
# These are used by GitHub Actions workflows for plugin submission processing

# Core dependencies (stdlib only needed for basic functionality)
# The parse, validate, and add scripts use only stdlib

# Optional security scanning
bandit>=1.7.0

# Optional for enhanced JSON schema validation
jsonschema>=4.0.0

# Optional for better HTTP handling
requests>=2.28.0

# Optional for async operations
aiohttp>=3.8.0

# Development/testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
