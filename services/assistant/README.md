# Assistant Service

The Assistant Service handles AI/ML interactions and provides intelligent responses for VoiceStudio.

## Endpoints

- `GET /health` - Service health check
- `GET /autofix/status` - Autofix service status

## Configuration

Configured to run on port 5080 by default.

## Development

This service is currently under development.

