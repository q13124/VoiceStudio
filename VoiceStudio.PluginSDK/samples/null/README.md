# Null Analyzer Sample

A minimal plugin exposing two operations:

- `analyze`: Computes RMS/peak for an input wav and produces a JSON report
- `ab_null`: Computes a null-difference wav between two inputs

This is a sketch; implement using your preferred language and gRPC.
