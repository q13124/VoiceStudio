# Backend API

Python FastAPI or Node.js Express backend.

## Endpoints

- `/api/voices/analyze` - Analyze voice profiles
- `/api/voices/clone` - Clone voice
- `/api/projects/{id}` - Project management
- `/ws` - WebSocket endpoint

## MCP Integration

The backend communicates with MCP servers through the `mcp_bridge` layer.

