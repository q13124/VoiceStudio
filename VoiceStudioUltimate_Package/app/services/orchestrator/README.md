# Orchestrator Service

The Orchestrator Service coordinates between different VoiceStudio services and manages workflow orchestration.

## Endpoints

- `GET /health` - Service health check
- `GET /settings` - Service configuration
- `GET /weights` - Model weights management

## Configuration

Configured to run on port 5090 by default.

## Development

This service is currently under development.

