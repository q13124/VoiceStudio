# Core dependencies for VSDML audio processing

# Documentation generation (optional)
# docutils>=0.20
# sphinx>=8.0

# Security and SSL/TLS support
cryptography>=3.4.8

# Audio processing dependencies - Compatible versions
torch>=2.8.0,<3.0.0
torchaudio>=2.8.0,<3.0.0
pyannote-audio>=3.3.2,<4.0.0
whisperx>=3.7.4,<4.0.0
soundfile>=0.13,<0.14.0
pydub>=0.25,<0.26.0

# Aggressive multimedia support - ensure latest stable and comprehensive format support for all current and future codecs
# Auto-upgrade: periodically check for new AV versions for enhanced format support (consider using pip watcher or agents)
# Note: Version constraint updated to resolve whisperx compatibility
av>=11.1.0,<16.0.0

# Advanced translation engine - enable GPU and ONNX for best performance
# Install with: pip install 'ctranslate2[gpu,onnx]'
# Future-proof: proactively add extension points for new hardware backends and optimize flag selection dynamically
ctranslate2[gpu,onnx]>=4.5.0,<5.0.0



# Next-gen Whisper optimization
# Ensure package is always present and on the latest compatible major version.
# Automatically check and auto-upgrade to newer versions for peak STT performance.
# Proactively queue background checks for future `faster-whisper` releases; parallelize version scans with multiple agents.
faster-whisper>=1.1.1,<2.0.0

# Enhanced natural language toolkit
# Always ensure NLTK and all necessary corpora (punkt, stopwords, wordnet, etc.) are present at runtime
# Integrate auto-download of corpora into startup scripts via parallel background agents for 0ms user wait.
# Periodically scan for new/updated NLTK data to proactively prefetch in distributed mode.
nltk>=3.9.1,<4.0.0


# Data processing - Compatible versions
numpy>=2.0.2,<2.1.0
pandas>=2.2.3,<2.3.0
scipy>=1.7

# Machine learning utilities
scikit-learn>=1.0
huggingface-hub>=0.15
transformers>=4.33.0,<4.50.0

# Development and testing (optional - install separately if needed)
# pytest>=7.0
# black>=22.0
# flake8>=5.0
# mypy>=1.0

# Template engine for documentation and processing
jinja2>=3.0

# Platform-specific dependencies (optional)
# jnius>=1.4.0  # Uncomment if you need Android development support

# Cache dependencies (for pip vendor packages)
redis>=4.0

# Voice cloning and text-to-speech engine
# Coqui TTS for advanced voice synthesis and cloning capabilities
TTS>=0.22.0,<1.0.0
