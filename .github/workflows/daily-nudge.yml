name: daily-nudge
on:
  schedule:
    - cron: "0 15 * * 1-5"   # 10:00 AM CDT on weekdays
  workflow_dispatch:

jobs:
  remind:
    runs-on: ubuntu-latest
    steps:
      - name: Post reminder
        run: |
          echo "Time for your 15-min sprint: open docs/15_MINUTE_WORKFLOW.md"
